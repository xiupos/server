apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yml
  - deployment.yml

secretGenerator:
  - name: cloudflared-token
    namespace: cloudflare-tunnel-jp
    env: ../../../../secrets/cloudflare-tunnel-jp.env
