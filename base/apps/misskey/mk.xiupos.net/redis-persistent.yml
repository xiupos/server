apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: misskey-mk-xiupos-net-redis-persistent
spec:
  chart: valkey
  repo: https://charts.bitnami.com/bitnami
  targetNamespace: misskey-mk-xiupos-net
  valuesContent: |- #yaml
    # https://github.com/bitnami/charts/tree/main/bitnami/valkey#readme

    fullnameOverride: redis-persistent
    architecture: standalone
    master:
      persistence:
        enabled: true
        storageClass: local-path
        size: 1Gi
      resources:
        requests:
          cpu: 50m
          memory: 32Mi
        limits:
          cpu: 500m
          memory: 128Mi
    auth:
      enabled: false
